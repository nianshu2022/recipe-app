# 计划：小程序与Web端图标按钮完全一致

## 背景
小程序已基本重建完成，但与Web端仍有若干图标/按钮差异需要修复，确保两端完全一致。

## 需要修改的文件

### 1. `miniapp/src/components/Icon/index.tsx` — 添加8个新图标
新增图标：`package`、`arrowRight`、`save`、`chevronLeft`、`mic`、`micOff`、`maximize`、`minimize`

### 2. `miniapp/src/pages/blind-box/index.tsx` — 4处修改
- 前面闲置态图标：`sparkles` → `package`（本地抽取模式）
- 揭晓结果封面回退：`chefHat` → `CATEGORY_ICONS[result.category]`
- "从我的菜谱抽取"按钮：`dice` → `package`
- "查看详情"按钮：添加 `arrowRight` 图标
- 添加"保存到我的菜谱"按钮（含 `save` 图标，仅推荐模式显示）

### 3. `miniapp/src/pages/shopping/index.tsx` — 1处修改
- 分类折叠箭头：从切换 `chevronDown`/`chevronRight` 改为始终 `chevronDown` + CSS旋转

### 4. `miniapp/src/pages/cooking/index.tsx` — 3处修改
- Header添加语音控制按钮（`mic`/`micOff`）和全屏按钮（`maximize`/`minimize`）
- "上一步"按钮：`arrowLeft` → `chevronLeft`
- 加载状态添加 `chefHat` 图标

### 5. `miniapp/src/pages/meal-plan/index.tsx` — 1处修改
- 菜谱选择器分类筛选：添加分类图标（`CATEGORY_ICONS`）

### 6. `src/pages/recipe/RecipeDetailPage.tsx`（Web端）— 1处修改
- 封面回退图标：通用 `ChefHat` → `categoryIcons[recipe.category]`（分类专属图标）

## 执行顺序
1. 先添加新图标到 Icon 组件
2. 逐页修改盲盒、购物清单、做菜、周餐计划页面
3. 修复Web端菜谱详情页封面回退
4. 构建验证：`npm run build:weapp`

## 验证
- `npm run build:weapp` 构建通过
- 逐页对比Web端和小程序端的图标使用是否一致
