{
  "name": "recipe-app-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute recipe-app-db --file=./src/db/schema.sql",
    "db:init:local": "wrangler d1 execute recipe-app-db --local --file=./src/db/schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.57.0"
  }
}
