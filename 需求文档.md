# 菜谱助手 - 需求梳理

## 一、产品定位

**目标用户**：00后，追求简约生活方式，喜欢自己做饭的年轻人

**核心价值**：
- 录入自己的拿手菜谱，建立个人菜谱库
- 做菜时快速查找，按步骤清晰操作
- 记录做饭历史，回顾生活点滴

**设计风格**：简约、大气、高级感（可参考：苹果设计语言、无印良品风格）

---

## 二、功能模块梳理

### 1. 菜谱管理模块

#### 1.1 录入菜谱
- **菜名**：中/英文名称
- **分类**：凉菜、热菜、汤羹、主食、甜品、饮品等
- **标签**：家常菜、快手菜、硬菜、减脂餐等
- **用料清单**：
  - 主料（如：鸡胸肉 300g）
  - 辅料（如：生姜 3片）
  - 调料（如：生抽 2勺）
- **操作步骤**：
  - 步骤序号
  - 步骤描述
  - 步骤图片（可选）
  - 注意事项/小贴士（可选）
- **其他信息**：
  - 烹饪时长
  - 难度等级（简单/中等/困难）
  - 参考人数（基准人数，如：2人份）
  - 封面图

#### 1.2 查看菜谱
- 列表展示（卡片式/瀑布流）
- 搜索功能（按菜名、标签、用料搜索）
- 筛选功能（按分类、难度、时长筛选）
- 排序功能（按时间、难度、名称）

### 2. 智能用料调整模块

#### 2.1 按人数调整
- 输入实际用餐人数
- 自动计算用料比例
- 显示调整后的用料清单

#### 2.2 计算规则
```
调整系数 = 实际人数 / 参考人数
新用量 = 原用量 × 调整系数
```

**示例**：
- 参考：2人份，鸡胸肉 300g
- 实际：4人份，鸡胸肉 = 300 × (4/2) = 600g

#### 2.3 边界处理
- 最小单位处理（如：0.5个鸡蛋 → 显示"少许"或"半个"）
- 调料类可设置不随人数变化

### 3. 做菜流程引导模块

#### 3.1 步骤展示
- 大字体显示当前步骤
- 步骤进度条
- 上一步/下一步切换
- 全屏模式（防止息屏）

#### 3.2 辅助功能
- 计时器（用于需要计时的步骤）
- 用料快速查看（悬浮窗/侧边栏）
- 语音播报（可选）

### 4. 做菜日历模块

#### 4.1 日历视图
- 月历展示
- 每日已做菜品标记
- 点击日期查看详情

#### 4.2 记录内容
- 做了什么菜
- 做菜时间
- 用餐人数
- 备注（可选）

#### 4.3 统计功能
- 本月做了几道菜
- 最常做的菜
- 做菜频率统计

### 5. 购物清单模块

#### 5.1 生成清单
- 从菜谱一键生成购物清单
- 选择多道菜谱后自动聚合所有食材
- 自动合并相同食材（如两道菜都需要"鸡胸肉"，合并数量）
- 智能去重：同食材不同表述的合并（如"鸡胸肉"和"鸡胸脯肉"）

#### 5.2 清单管理
- 按食材类别分组展示（蔬菜区、肉类区、调料区、干货区等）
- 支持手动添加自定义采购项
- 勾选已购买，已购项置灰或折叠
- 支持修改数量（临时调整采购量）
- 清单历史记录（可复用上次清单）

#### 5.3 关联功能
- 可从"周餐计划"一键生成本周购物清单
- 可从"冰箱食材库"自动排除已有食材，只生成缺漏清单

### 6. 周餐计划模块

#### 6.1 周视图
- 按周展示周一到周日的餐食安排
- 每天分为早餐、午餐、晚餐、加餐四个时段
- 支持拖拽菜谱到对应日期和时段

#### 6.2 计划管理
- 从菜谱库中选择菜谱填入计划
- 支持创建计划模板（如"减脂周"、"工作日快手餐"）
- 模板复用：一键套用历史计划
- 未安排的日期显示推荐菜谱

#### 6.3 联动功能
- 周计划 → 一键生成本周购物清单
- 周计划 → 同步到做菜日历
- 周计划 → 营养统计汇总（配合营养模块）

### 7. 冰箱/食材库管理模块

#### 7.1 食材录入
- 快速录入：从常用食材预设列表中点选
- 手动输入：自定义食材名称和数量
- 分类管理：蔬菜、水果、肉类、海鲜、蛋奶、调料、干货等

#### 7.2 库存管理
- 记录食材数量和单位
- 记录购入日期和保质期
- 临期提醒（如：还剩2天过期的食材高亮提示）
- 已用完/已过期食材自动归档

#### 7.3 智能推荐
- 根据冰箱现有食材，推荐可制作的菜谱（按匹配度排序）
- "冰箱里有什么就做什么"模式
- 缺少少量食材时提示"再买X样就能做Y菜"

### 8. 营养信息模块

#### 8.1 营养计算
- 根据菜谱用料自动估算热量（千卡）
- 显示主要营养素：蛋白质、碳水化合物、脂肪、膳食纤维
- 按每份（per serving）展示
- 支持调整份量后营养数据联动更新

#### 8.2 营养展示
- 菜谱详情页展示营养成分卡片
- 用可视化图表展示营养素占比（饼图/环形图）
- 热量等级标签（低卡 <300kcal、适中 300-600kcal、高卡 >600kcal）

#### 8.3 健康筛选
- 按热量范围筛选菜谱
- 按营养目标筛选（高蛋白、低碳水、低脂等）
- 与"减脂餐"、"增肌餐"等标签联动

### 9. 视频/多媒体步骤模块

#### 9.1 视频步骤
- 步骤支持短视频（15-30秒）替代或补充静态图片
- 关键手法用短视频展示（翻锅、揉面、刀工等）
- 视频自动播放、循环播放、静音播放

#### 9.2 媒体管理
- 支持从相册选取视频
- 支持录制短视频（调用设备摄像头）
- 视频压缩与裁剪（控制存储大小）
- 图片/视频混合排序

### 10. 菜谱分享与社区模块

#### 10.1 菜谱分享
- 导出菜谱为精美图片（适合发朋友圈/小红书）
- 生成分享链接（包含完整菜谱信息）
- 通过微信、系统分享等方式发送

#### 10.2 "做作业"功能
- 查看他人菜谱后，按步骤完成并上传成品照片
- 成品照片展示在菜谱详情页的"作业"区域
- 支持点赞和评论

#### 10.3 评分与反馈
- 菜谱评分（1-5星）
- 文字评论
- "我做了"计数

### 11. 场景化与智能推荐模块

#### 11.1 场景分类
- 按用餐场景：早餐、午餐、晚餐、下午茶、夜宵
- 按生活方式：快手菜（<30分钟）、懒人餐、减脂餐、增肌餐
- 按场合：家常便饭、宴客大餐、节日特辑、便当带饭
- 按烹饪方式：蒸、煮、炒、烤、炖、凉拌

#### 11.2 时令推荐
- 按季节推荐应季菜谱（春天野菜、夏天凉菜、秋天炖汤、冬天火锅）
- 节气专题（冬至饺子、端午粽子、中秋月饼）
- 节日专题（年夜饭、生日蛋糕、情人节晚餐）

#### 11.3 个性化推荐
- 根据浏览和收藏历史推荐相似菜谱
- 根据做菜记录推荐新菜（避免重复）
- "猜你想做"首页推荐区

### 12. 收藏夹/菜单系统

#### 12.1 收藏夹管理
- 支持创建多个收藏夹（如"周末大餐"、"快手早餐"、"减脂餐"）
- 一道菜谱可属于多个收藏夹
- 收藏夹封面自动生成（取第一道菜的封面图）
- 支持排序（按添加时间、菜名、难度）

#### 12.2 菜单功能
- 将多道菜组合为一个"菜单"（如"四菜一汤家宴菜单"）
- 菜单可整体分享
- 菜单可一键加入周餐计划
- 菜单可一键生成购物清单

### 13. 语音控制模块

#### 13.1 语音指令
- "下一步" / "上一步" — 切换步骤
- "开始计时 X 分钟" — 启动计时器
- "暂停" / "继续" — 控制计时器
- "重复" — 重新播报当前步骤
- "用料" — 语音播报当前步骤所需食材

#### 13.2 实现方式
- 基于 Web Speech API（SpeechRecognition + SpeechSynthesis）
- 做菜引导页常驻麦克风监听
- 语音反馈：识别到指令后语音确认（如"好的，下一步"）
- 支持开关语音控制功能

---

## 三、页面结构设计

```
欢迎/引导页（首次使用）
├── 欢迎插图 + 品牌语
├── 选择感兴趣菜系
├── 导入示例菜谱
└── 进入首页

首页（菜谱库）
├── 搜索栏
├── 场景/时令推荐区
├── 分类标签
├── 菜谱卡片列表
│   ├── 封面图
│   ├── 菜名
│   ├── 难度/时长
│   ├── 营养标签（热量等级）
│   └── 快捷操作（做菜/编辑/删除/收藏）
└── 底部导航
    ├── 菜谱库（首页）
    ├── 冰箱食材
    ├── 周餐计划
    ├── 做菜日历
    └── 我的

菜谱详情页
├── 封面图/视频
├── 菜名 + 基本信息（难度、时长、份量）
├── 营养成分卡片（热量、蛋白质、碳水、脂肪）
├── 用料清单（支持人数调整）
├── 操作步骤（图文/视频混合）
├── 作业区（成品照片、评论、评分）
└── 底部按钮（开始做菜 / 加入收藏夹 / 加入周计划 / 生成购物清单 / 分享）

做菜引导页
├── 进度条
├── 当前步骤（大字体）
├── 步骤图片/视频
├── 计时器（如有）
├── 用料快速查看（悬浮窗）
├── 语音控制状态指示
├── 上一步/下一步
└── 完成按钮

做菜日历页
├── 月历视图
├── 今日/选中日期的菜谱列表
└── 统计概览（月度/年度数据、菜系偏好、做饭频率趋势）

购物清单页
├── 清单来源（关联菜谱/周计划）
├── 按类别分组的食材列表
├── 已购/未购切换
├── 手动添加项
└── 底部按钮（全部清除 / 分享清单）

周餐计划页
├── 周视图（周一~周日 × 早午晚餐）
├── 拖拽/点击添加菜谱
├── 计划模板管理
└── 底部按钮（生成购物清单 / 同步到日历）

冰箱/食材库页
├── 食材分类列表
├── 临期提醒区
├── 快速录入入口
└── 推荐菜谱（基于现有食材）

收藏夹/菜单页
├── 收藏夹列表（封面 + 菜谱数）
├── 菜单列表
└── 新建收藏夹/菜单

我的页面
├── 个人信息
├── 我的收藏夹
├── 我的菜单
├── 购物清单历史
├── 周计划模板
├── 营养报告（周/月汇总）
├── 做饭年度报告
├── 设置
│   ├── 账号与同步（登录、同步状态、同步开关）
│   ├── 外观（浅色/深色/跟随系统）
│   ├── 语音控制开关
│   ├── 营养数据源设置
│   ├── 数据导出/导入
│   ├── 存储管理（本地缓存大小、清理）
│   ├── 无障碍设置（字号大小、减少动画）
│   └── 关于
└── 关于

添加/编辑菜谱页
├── 基本信息（菜名、分类、标签）
├── 封面图上传
├── 用料清单（支持增删，含营养信息录入）
├── 操作步骤（支持增删、排序，图片/视频混合）
├── 营养信息（自动计算或手动修正）
└── 保存按钮
```

---

## 四、技术选型建议

| 方向 | 推荐方案 | 备选方案 |
|------|---------|---------|
| 前端框架 | React 18 + TypeScript | Vue 3 |
| UI组件库 | Tailwind CSS + Shadcn/ui | Ant Design Mobile |
| 状态管理 | Zustand | Redux Toolkit |
| 本地存储 | IndexedDB（离线缓存） | LocalStorage |
| 云端数据库 | Cloudflare D1（SQLite） | Supabase / PlanetScale |
| 后端 API | Cloudflare Workers | Vercel Serverless / AWS Lambda |
| 对象存储 | Cloudflare R2（图片/视频） | AWS S3 / 阿里云 OSS |
| 同步策略 | 乐观更新 + 后台同步 | 实时 WebSocket |
| 构建工具 | Vite | Webpack |
| 日历组件 | react-calendar | FullCalendar |
| 拖拽排序 | @dnd-kit/core | react-beautiful-dnd |
| 图表可视化 | Recharts | ECharts |
| 视频播放 | 原生 HTML5 Video | video.js |
| 语音识别 | Web Speech API | 第三方语音 SDK |
| 图片压缩 | browser-image-compression | 原生 Canvas |
| 数据导出 | FileSaver.js + jsPDF | html2canvas |
| 主题切换 | CSS 变量 + Tailwind dark: | next-themes |
| 测试框架 | Vitest + React Testing Library | Jest |
| E2E 测试 | Playwright | Cypress |
| CI/CD | GitHub Actions + Cloudflare Pages | Vercel / Netlify |
| 包管理器 | pnpm | npm / yarn |

**混合存储策略**：IndexedDB 作为本地缓存层，保证离线可用；Cloudflare D1 作为云端主存储，实现多设备同步。在线时自动同步，离线时操作本地缓存，联网后自动合并。Cloudflare R2 存储图片/视频等大文件。详见第七节"数据同步架构"。

---

## 五、功能优先级与开发阶段

### 第一阶段：核心功能（MVP）
> 目标：可用的个人菜谱工具

- [x] 菜谱管理（CRUD、分类、标签、搜索、筛选）
- [x] 智能用料调整（按人数缩放）
- [x] 做菜流程引导（步骤、计时器、全屏模式）
- [x] 做菜日历（记录、统计）
- [ ] 收藏夹/菜单系统（低开发成本，管理菜谱必备）

### 第二阶段：实用工具
> 目标：提升日常做饭效率

- [ ] 购物清单（从菜谱/菜单生成，分类管理，合并去重）
- [ ] 语音控制做菜（Web Speech API，做菜时手上有油的刚需）
- [ ] 菜谱分享（导出图片/链接，系统分享）

### 第三阶段：规划与健康
> 目标：从单道菜谱扩展到饮食规划

- [ ] 周餐计划（周视图、拖拽排餐、模板复用）
- [ ] 冰箱/食材库管理（库存录入、临期提醒、智能推荐）
- [ ] 营养信息计算（热量估算、营养素展示、健康筛选）
- [ ] 场景化与智能推荐（场景分类、时令推荐、个性化推荐）

### 第四阶段：内容升级
> 目标：丰富内容形式，提升体验

- [ ] 视频/多媒体步骤（短视频录制与播放）
- [ ] 做饭年度报告（年度数据、菜系偏好、成长曲线）
- [ ] "做作业"功能（成品照片、点赞评论、评分）

### 第五阶段：后端与云同步
> 目标：搭建 Cloudflare 后端，实现多设备数据同步

- [ ] 搭建 Cloudflare Workers API 服务
- [ ] 设计并创建 Cloudflare D1 数据库表结构
- [ ] 实现用户认证（Cloudflare Access 或自建 JWT）
- [ ] 实现 IndexedDB ↔ D1 双向同步机制
- [ ] 接入 Cloudflare R2 存储图片/视频
- [ ] 离线队列：离线操作本地缓存，联网后自动同步
- [ ] 冲突解决策略（last-write-wins / 字段级合并）

### 第六阶段：远期扩展
> 目标：社区与 AI 能力

- [ ] 社区功能（菜谱广场、用户关注、热门排行）
- [ ] AI 识别食材推荐菜谱（图像识别 + 推荐算法）
- [ ] 智能厨房设备联动

---

## 六、数据模型设计（概要）

所有实体均包含同步字段：`syncStatus`（synced / pending / conflict）、`updatedAt`、`deletedAt`（软删除）。

```
Recipe（菜谱）
├── id, userId, name, category, tags[]
├── coverImage (R2 URL), difficulty, duration, servings
├── ingredients[]: { name, amount, unit, type(主料/辅料/调料), scalable }
├── steps[]: { order, description, image(R2 URL), video(R2 URL), tip, timer }
├── nutrition: { calories, protein, carbs, fat, fiber }
├── syncStatus, createdAt, updatedAt, deletedAt

Collection（收藏夹）
├── id, userId, name, coverImage, recipeIds[]
├── syncStatus, createdAt, updatedAt, deletedAt

Menu（菜单）
├── id, userId, name, recipeIds[]
├── syncStatus, createdAt, updatedAt, deletedAt

MealPlan（周餐计划）
├── id, userId, weekStart, days[7]: { breakfast, lunch, dinner, snack: recipeId }
├── syncStatus, createdAt, updatedAt, deletedAt

ShoppingList（购物清单）
├── id, userId, sourceRecipeIds[], items[]: { name, amount, unit, category, checked }
├── syncStatus, createdAt, updatedAt, deletedAt

FridgeItem（冰箱食材）
├── id, userId, name, amount, unit, category, purchaseDate, expiryDate
├── syncStatus, createdAt, updatedAt, deletedAt

CookingRecord（做菜记录）
├── id, userId, recipeId, date, servings, notes
├── syncStatus, createdAt, updatedAt, deletedAt

User（用户）
├── id, email, nickname, avatar(R2 URL)
├── createdAt, updatedAt

UserSettings（用户设置）
├── userId, voiceControl, nutritionSource, syncEnabled, theme
├── syncStatus, updatedAt
```

---

## 七、数据同步架构

### 7.1 整体架构

```
┌─────────────────────────────────────────────────┐
│                   前端 (PWA)                      │
│  ┌───────────┐  ┌───────────┐  ┌──────────────┐ │
│  │  React UI  │  │  Zustand   │  │  Sync Engine │ │
│  └───────────┘  └─────┬─────┘  └──────┬───────┘ │
│                       │               │          │
│              ┌────────▼───────────────▼────────┐ │
│              │        IndexedDB (本地缓存)       │ │
│              └────────────────┬────────────────┘ │
└───────────────────────────────┼──────────────────┘
                                │ HTTPS
                    ┌───────────▼───────────┐
                    │  Cloudflare Workers    │
                    │  (REST API Layer)      │
                    └───────┬───────┬───────┘
                            │       │
                ┌───────────▼┐  ┌───▼──────────┐
                │ Cloudflare  │  │   Cloudflare  │
                │ D1 (SQLite) │  │   R2 (文件)   │
                └─────────────┘  └──────────────┘
```

### 7.2 同步策略

**离线优先（Offline-First）**
- 所有读写操作优先走 IndexedDB，保证离线可用
- 写操作同时记录到同步队列（sync queue）
- 联网后由 Sync Engine 自动将队列中的操作推送到 D1

**同步流程**
```
写操作流程：
  用户操作 → 写入 IndexedDB → 标记 syncStatus=pending → 加入同步队列
  → 网络可用时 → POST /api/sync → 写入 D1 → 标记 syncStatus=synced

读操作流程：
  页面加载 → 读取 IndexedDB（即时展示）→ 后台 GET /api/sync?since=lastSync
  → 增量更新 IndexedDB → UI 自动刷新
```

**冲突解决**
- 默认采用 Last-Write-Wins（以 updatedAt 最新的为准）
- 删除操作使用软删除（deletedAt），同步后物理删除
- 未来可升级为字段级合并（针对大文本字段如步骤描述）

### 7.3 Cloudflare Workers API 设计

```
认证相关：
  POST   /api/auth/register     注册
  POST   /api/auth/login        登录
  POST   /api/auth/refresh      刷新 Token

菜谱相关：
  GET    /api/recipes            获取菜谱列表
  POST   /api/recipes            创建菜谱
  PUT    /api/recipes/:id        更新菜谱
  DELETE /api/recipes/:id        删除菜谱（软删除）
  POST   /api/recipes/:id/media  上传图片/视频到 R2

同步相关：
  GET    /api/sync?since=timestamp   增量拉取变更
  POST   /api/sync                    批量推送本地变更
  POST   /api/sync/upload-url         获取 R2 预签名上传地址

其他实体 CRUD：
  /api/collections        收藏夹
  /api/menus              菜单
  /api/meal-plans         周餐计划
  /api/shopping-lists     购物清单
  /api/fridge-items       冰箱食材
  /api/cooking-records    做菜记录
  /api/settings           用户设置
```

### 7.4 Cloudflare 免费额度

| 资源 | 免费额度 | 预估用量 |
|------|---------|---------|
| Workers 请求 | 10 万次/天 | 个人使用绰绰有余 |
| D1 读取 | 500 万次/天 | 远超需求 |
| D1 写入 | 10 万次/天 | 远超需求 |
| D1 存储 | 5 GB | 可存储数万道菜谱 |
| R2 存储 | 10 GB | 图片视频够用 |
| R2 出口流量 | 免费 | 无额外费用 |

### 7.5 开发与部署

- **开发环境**：使用 `wrangler dev` 本地模拟 D1 + Workers
- **部署方式**：`wrangler deploy` 一键部署到 Cloudflare 边缘节点
- **前端部署**：Cloudflare Pages（自动从 Git 仓库构建部署）
- **域名**：支持绑定自定义域名

---

## 八、PWA 配置与离线体验

### 8.1 PWA 基础配置

```
manifest.json 配置项：
├── name: "菜谱助手"
├── short_name: "菜谱"
├── start_url: "/"
├── display: "standalone"
├── theme_color: "#ffffff"
├── background_color: "#ffffff"
├── orientation: "portrait"
└── icons[]: 192x192, 512x512, maskable
```

### 8.2 Service Worker 策略

- **静态资源**：Cache-First（HTML/CSS/JS 预缓存）
- **API 请求**：Network-First（优先网络，离线时返回缓存）
- **图片/视频**：Stale-While-Revalidate（先用缓存，后台更新）
- **版本更新**：新版本可用时提示用户刷新，支持跳过等待

### 8.3 离线体验

- 离线时所有已缓存的菜谱正常浏览和使用
- 离线操作（增删改）记录到同步队列，联网后自动同步
- 离线状态指示器（顶部 banner 提示"当前为离线模式"）
- 做菜引导页全程离线可用（核心场景）

### 8.4 安装引导

- 首次访问时弹出"添加到主屏幕"引导
- 检测是否已安装，已安装则不再提示
- 引导样式：底部浮层，不遮挡主要内容

---

## 九、用户体验细节

### 9.1 首次使用引导

- 新用户首次进入展示欢迎页，引导创建第一道菜谱
- 提供 3-5 道预置示例菜谱（如番茄炒蛋、可乐鸡翅），可一键导入
- 引导流程：欢迎 → 选择感兴趣菜系 → 导入示例菜谱 → 进入首页

### 9.2 空状态设计

每个页面在无数据时展示友好的空状态：

```
菜谱库（空）：插图 + "还没有菜谱，快来创建你的第一道拿手菜吧" + [创建菜谱] 按钮
收藏夹（空）：插图 + "还没有收藏夹，去菜谱库逛逛吧" + [去逛逛] 按钮
购物清单（空）：插图 + "清单是空的，从菜谱生成一份吧" + [选择菜谱] 按钮
周餐计划（空）：插图 + "新的一周，安排点好吃的吧" + [开始规划] 按钮
冰箱食材（空）：插图 + "冰箱空空如也，录入你买了什么吧" + [录入食材] 按钮
做菜日历（空）：插图 + "还没有做菜记录，下厨试试吧"
```

### 9.3 加载状态

- **骨架屏**：菜谱列表、详情页等使用骨架屏占位，避免白屏
- **按钮加载态**：保存、同步等操作按钮显示 loading spinner，防止重复点击
- **图片渐入**：图片加载完成后淡入显示（opacity transition）
- **下拉刷新**：列表页支持下拉刷新同步最新数据

### 9.4 错误状态

- **网络错误**：顶部 Toast 提示"网络不可用，已切换到离线模式"
- **同步失败**：图标角标显示待同步数量，设置页可查看失败详情并重试
- **上传失败**：图片/视频上传失败时保留本地数据，支持一键重试
- **页面错误**：通用错误页，提供"刷新重试"按钮

### 9.5 深色模式

- 支持三种模式：浅色 / 深色 / 跟随系统
- 设置页切换，实时生效，无需刷新
- 做菜引导页深色模式下降低屏幕亮度，减少刺眼
- 深色模式配色：
  - 背景：#0a0a0a / #171717
  - 卡片：#1e1e1e / #262626
  - 文字：#e5e5e5 / #a3a3a3
  - 强调色保持品牌色不变

---

## 十、安全与质量保障

### 10.1 输入校验与安全

**前端校验**
- 所有表单字段：必填项校验、长度限制、格式校验
- 菜谱名称：最长 50 字符
- 步骤描述：最长 1000 字符
- 图片上传：仅允许 jpg/png/webp，单张最大 5MB
- 视频上传：仅允许 mp4，单个最大 50MB

**后端校验（Workers）**
- API 参数严格校验（类型、范围、长度）
- SQL 注入防护：使用 D1 参数化查询
- XSS 防护：用户输入渲染时转义
- CORS 配置：仅允许指定域名
- Rate Limiting：API 请求频率限制（防刷）

**认证安全**
- JWT Token 存储在 HttpOnly Cookie（非 localStorage）
- Access Token 短有效期（15 分钟），Refresh Token 长有效期（7 天）
- 密码使用 bcrypt 哈希存储
- 敏感操作（删除菜谱、修改密码）需要二次确认

### 10.2 图片/文件处理

```
上传限制：
├── 封面图：最大 5MB，推荐 800x600，自动压缩
├── 步骤图片：最大 5MB，自动压缩至 1200px 宽
├── 步骤视频：最大 50MB，自动转码压缩
├── 成品照片：最大 5MB，自动压缩
└── 支持格式：jpg, png, webp（图片）/ mp4（视频）

处理流程：
  选择文件 → 前端压缩（browser-image-compression）→ 上传到 R2
  → 返回 CDN URL → 保存到菜谱数据
```

### 10.3 数据备份与恢复

- **导出全部数据**：将所有 IndexedDB 数据导出为 JSON 文件下载
- **导入数据**：从 JSON 文件恢复数据（合并或覆盖可选）
- **导出单道菜谱**：导出为独立 JSON 文件，可分享给他人导入
- **数据统计**：设置页显示本地数据大小、菜谱数量、图片占用空间

### 10.4 无障碍设计（A11y）

- **语义化 HTML**：正确使用 h1-h6、nav、main、article、button 等标签
- **键盘导航**：所有交互元素可通过 Tab 键访问，Enter/Space 触发
- **ARIA 标签**：图标按钮添加 aria-label，动态区域添加 aria-live
- **对比度**：文字与背景对比度 ≥ 4.5:1（WCAG AA 标准）
- **字号**：正文最小 16px，做菜引导页步骤文字 ≥ 24px
- **焦点样式**：自定义 focus-visible 样式，清晰可见
- **减少动画**：尊重 prefers-reduced-motion 系统设置

---

## 十一、测试策略

### 11.1 单元测试

- **工具**：Vitest + React Testing Library
- **覆盖范围**：
  - 工具函数（用料缩放计算、营养计算、食材合并去重）
  - Zustand Store（状态管理逻辑）
  - 同步引擎（冲突解决、队列处理）
  - 自定义 Hooks
- **目标覆盖率**：核心逻辑 ≥ 80%

### 11.2 组件测试

- **工具**：Vitest + React Testing Library + jsdom
- **覆盖范围**：
  - 关键组件渲染正确性
  - 用户交互（点击、输入、拖拽）
  - 表单验证逻辑
  - 空状态 / 错误状态展示

### 11.3 E2E 测试

- **工具**：Playwright
- **核心场景**：
  - 创建菜谱 → 查看详情 → 开始做菜 → 完成
  - 搜索/筛选菜谱
  - 生成购物清单
  - 离线模式 → 联网同步
  - 深色模式切换
- **执行时机**：PR 合并前自动运行

### 11.4 测试文件结构

```
src/
├── utils/__tests__/           工具函数测试
├── stores/__tests__/          Store 测试
├── hooks/__tests__/           Hooks 测试
├── components/__tests__/      组件测试
└── __e2e__/                   E2E 测试
```

---

## 十二、CI/CD 与部署

### 12.1 代码规范

- **ESLint**：React + TypeScript 规则集
- **Prettier**：统一代码格式
- **Husky + lint-staged**：提交前自动检查和格式化

### 12.2 GitHub Actions 工作流

```
Push / PR 触发：
├── 1. Lint 检查（ESLint + Prettier）
├── 2. 单元测试（Vitest）
├── 3. 组件测试（Vitest + jsdom）
├── 4. 构建检查（Vite build）
└── 5. E2E 测试（Playwright，仅 PR 时）

main 分支推送触发：
├── 1-4 同上
├── 5. 部署前端到 Cloudflare Pages
└── 6. 部署 Workers 到 Cloudflare
```

### 12.3 部署架构

```
┌──────────────┐     Push      ┌──────────────────┐
│  GitHub Repo  │ ──────────► │  GitHub Actions   │
└──────────────┘              └────────┬─────────┘
                                       │
                          ┌────────────┼────────────┐
                          ▼            ▼            ▼
                  ┌──────────────┐ ┌────────┐ ┌────────┐
                  │ Cloudflare   │ │ D1     │ │ R2     │
                  │ Pages (前端) │ │ (数据库)│ │ (文件) │
                  └──────────────┘ └────────┘ └────────┘
```

- **前端**：Cloudflare Pages 自动构建部署，支持 Preview 环境
- **后端**：`wrangler deploy` 部署 Workers
- **数据库**：D1 通过 wrangler migrations 管理表结构变更
- **环境变量**：通过 Cloudflare Dashboard 或 wrangler.toml 管理
